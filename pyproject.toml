[tool.ruff]
line-length = 100
target-version = "py310"
extend-exclude = ["logs", "data", "data_clean"]

[tool.ruff.lint]
select = ["E", "F", "I", "W", "UP", "B", "SIM"]
ignore = ["E501"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-ra -q"
pythonpath = ["."]
