database:
  host: localhost
  port: 5432
  user: airflow
  password: airflow
  dbname: airflow

paths:
  raw_data: data/raw/
  # Directory that the Spark transform writes cleaned_train.csv / cleaned_test.csv into,
  # and that etl/load.py reads from.
  processed_data: data/processed/
  spark_configs: spark-configs/

s3:
  endpoint: http://minio:9000
  bucket: nyc-taxi
  raw_prefix: raw/

logging:
  level: INFO
  file: logs/etl.log
